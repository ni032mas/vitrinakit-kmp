# compileSdk is 36 (Android 16), whose Robolectric shadows require Java 21. This module is built
# on the pinned JDK 17 toolchain (see root build.gradle.kts), so run the Robolectric sandbox
# against API 34 instead: nothing exercised by these tests is 35/36-specific.
sdk=34
